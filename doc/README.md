# Documentación — Las Gambusinas Launcher

Panel de escritorio **Electron** (Windows) para operar el ecosistema local del proyecto **Las Gambusinas**: backend Node, app web cocina, cliente Expo (mozos), MongoDB, Git, datos `data/` y builds EAS Android.

| Documento | Contenido |
|-----------|-----------|
| [PROYECTO.md](./PROYECTO.md) | Objetivo, alcance, arquitectura, configuración, IPC, flujos, empaquetado y referencias de código |

**Ubicación en repo:** `launcher/` (esta carpeta `doc/` vive junto a `src/`, `renderer/`, `package.json`).
